"python3 -m venv venv". 
Then I activated it with "source venv/bin/activate".
Then installed requirements "pip install -r requirements.txt".
To run the api: "uvicorn app.main:app --reload --port 8000".